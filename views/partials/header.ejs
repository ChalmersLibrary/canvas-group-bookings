<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">BOKNINGSVERKTYG</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarText">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link" href="/">Tillgängliga tider</a>
        </li>
        <% if (!user.isAdministrator && !user.isInstructor) { %>
          <li class="nav-item">
            <a class="nav-link" href="/reservations">Mina bokningar <span id="statsReservationsCount" class="badge badge-pill badge-success"></span></a>
          </li>
        <% } %>
        <% if (user.isInstructor) { %>
          <li class="nav-item">
            <a class="nav-link" href="/instructor/upcoming">Mina tillfällen <span id="statsInstructorSlotCount" class="badge badge-pill badge-success"></span></a>
          </li>
        <% } %>
        <% if (user.isAdministrator) { %>
          <li class="nav-item">
            <a class="nav-link" href="/admin">Administration</a>
          </li>
        <% } %>
      </ul>
      <span class="navbar-text">
        <%= user.name %>
        <% if (user.isAdministrator) { %>
          <strong>(administratör)</strong>
        <% } else if (user.isInstructor) { %>
          <strong>(lärare)</strong>
        <% } else { %>
          <% if (user.groups_human_readable && user.groups_human_readable.length) { %>
            <strong>(<%= user.groups_human_readable.join(", ") %>)</strong>  
          <% } else { %>
            <strong>(ingen grupp)</strong>
          <% } %>
        <% } %>
      </span>
    </div>
  </div>
</nav>